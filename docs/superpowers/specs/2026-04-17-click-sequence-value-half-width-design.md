# 点击步骤设值半行布局设计

## 背景

点击步骤编辑器已经把字段改为行内标签布局，但 `设值` 仍占整行，纵向空间仍偏高。

## 目标

- 保持 `选择器` 独占整行
- 将 `设值` 改为半行宽度
- 让 `设值` 与 `设值模式` 在同一行配对显示

## 非目标

- 不修改字段顺序
- 不改变保存逻辑或运行时行为
- 不调整其他字段的两列布局

## 方案

只调整点击步骤编辑器的局部样式：移除 `设值` 的整行跨列规则，保留 `选择器` 为整行。为避免再次被通用 `.form-grid label` 覆盖，继续使用高优先级的点击步骤专用选择器。
